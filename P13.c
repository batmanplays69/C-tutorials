#include <stdio.h>

int main()
{
    int age=5;
    if (age>10)
    {
        printf("Your age is greater than 10");
    }
    // there will be no output as age is less than 10, so dont worry , your code is working
    return 0;
}