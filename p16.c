#include <stdio.h>

int main()
{
    int a=0 , b=1;
    printf("the value of a or b is %d", a&&b);// && is logical AND operator , if both statements are true , then it returns true value
    // if any one statement is false , then it returns false value
    // 0 is false value , and any non-zero value is true value
    
    return 0;
}